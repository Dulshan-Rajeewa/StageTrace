feature_flags:
  email_rate_limit_enabled: false
  new_onboarding_flow: true

services:
  email:
    provider: sendgrid
    timeout_ms: 3000
    max_retries: 3
  database:
    host: staging-db.internal
    port: 5432
    pool_size: 10

dependencies:
  python: "3.11"
  fastapi: "0.136.1"